# Branchs in git

Branchs are also incredibly light. They are pointer for specific commits.

git branch -> Creates a new branch but the HEAD is in the previous branch. The HEAD remains in the other branch.
git checkout <branch> -> HEAD moves to branch
git checkout -b <branch> -> The merge of the above commands.
