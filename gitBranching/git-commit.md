# Commits in Git 

A commit is a snapshot of all the files in your directory.
It can compress a commit as a set of changes between a version of your repository and the next one. Git also keeps a history of when each commit ocurred.
